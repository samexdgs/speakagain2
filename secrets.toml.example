# SpeakAgain v2.0 — paste into Streamlit Cloud → App settings → Secrets
# Or save as .streamlit/secrets.toml for local dev (never commit)

BREVO_API_KEY = "xkeysib-YOUR-KEY-HERE"

# Optional — app works without this using rule-based fallback
ANTHROPIC_API_KEY = "sk-ant-api03-YOUR-KEY-HERE"
